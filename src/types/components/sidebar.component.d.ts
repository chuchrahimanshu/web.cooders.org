interface SidebarTransitionProps {
  sidebarToggle: boolean;
}

interface SidebarProps {
  sidebarGroupToggle: string;
  handleSidebarGroupClick: (id: string) => void;
}

interface SidebarIconProps {
  sidebarGroupToggle: string;
  handleSidebarGroupClick: (id: string) => void;
}

interface SidebarBodyProps {
  sidebarGroupToggle: string;
  handleSidebarGroupClick: (id: string) => void;
}